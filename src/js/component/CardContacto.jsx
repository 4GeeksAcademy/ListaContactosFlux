/*import React from "react";
import { Link } from "react-router-dom";

const CardContactos = ({ contacto, borrarContacto }) => { // recibe props desestructurados. Aquí contacto representa los datos de cada contacto y borrarContacto
  return (
    <div className="card mb-3">
      <div className="card-body">
        <h5 className="card-title">{contacto.name}</h5> //Muestro el nombre del contacto con la clase 
        <h6 className="card-subtitle mb-2 text-muted">{contacto.phone}</h6> // Muestro el teléfono del contacto
        <p className="card-text">{contacto.email}</p> //Muestro el teléfono el email
        <p className="card-text">{contacto.address}</p> // Muestro la dirección
        <button className="btn btn-danger" onClick={() => borrarContacto(contacto.id)}>
          Borrar contacto
        </button>
      </div>
    </div>
  );
};

export default CardContactos;*/
